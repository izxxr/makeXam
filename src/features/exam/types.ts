export default interface Exam {
    title: string,
    subtitle: string,
    instructionsHeader: string,
    frontPageFormat: boolean,
    includeBranding: boolean,
    includePageNumbers: boolean,
    includeGradingTable: boolean,
}
